# Gloop Examples

This directory contains an example project with representative Bazel rules.

Its primary purposes are:

1.  To exercise Gloop's `external_transforms.bara.sky`, helping to ensure that
    representative Bazel rules are transformed correctly.
1.  To enable verification in CI that development dependencies of Gloop are not
    required for Gloop's clients.
