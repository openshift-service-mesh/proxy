"""Bazel module for toml.bzl smoke tests."""

module(name = "toml_smoke_test")

bazel_dep(name = "toml.bzl", version = "0.0.0")
bazel_dep(name = "re.bzl", version = "0.1.0")
bazel_dep(name = "platforms", version = "1.0.0")

local_path_override(
    module_name = "toml.bzl",
    path = "../..",
)
