"""Public build rules for TOML library."""

load("@bazel_skylib//:bzl_library.bzl", "bzl_library")

package(default_visibility = ["//visibility:public"])

bzl_library(
    name = "toml",
    srcs = ["toml.bzl"],
    deps = [
        "//toml/private:decode",
        "//toml/private:encode",
    ],
)
